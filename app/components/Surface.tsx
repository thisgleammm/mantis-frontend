"use client";

import { Surface as HeroSurface } from "@heroui/react";

/**
 * Surface Component
 * 
 * A semantic container that provides different levels of visual prominence.
 * Wraps HeroUI's Surface component for consistent usage across the app.
 */
export const Surface = HeroSurface;
